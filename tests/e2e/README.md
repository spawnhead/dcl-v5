# E2E Smoke Tests (TASK-0087)

Tests live in `modern/ui/tests/e2e/` for module resolution.

Run from `modern/ui`:

```bash
cd modern/ui
npx playwright test
```

Prerequisites: UI :5173, backend :8080, Postgres up.
