# DCL Modern — MCP / env vars (NO SECRETS COMMITTED)
# Copy to .env and fill; .env is gitignored.

# Postgres (matches ops/docker-compose.yml)
DCL_PG_HOST=localhost
DCL_PG_PORT=5432
DCL_PG_DB=dcl
DCL_PG_USER=dcl
DCL_PG_PASSWORD=

# GitHub (optional, for GitHub MCP)
# GITHUB_TOKEN=
