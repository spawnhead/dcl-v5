version: "3.8"

services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: dcl
      POSTGRES_USER: dcl
      POSTGRES_PASSWORD: dcl
    volumes:
      - dcl_postgres_data:/var/lib/postgresql/data

volumes:
  dcl_postgres_data:
