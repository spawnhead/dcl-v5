# Contractors list — Payloads

## Placed here after implementation
- `lookups-response.json` — GET /api/contractors/lookups
- `data-request.json` — POST /api/contractors/data (filter)
- `data-response.json` — POST /api/contractors/data (items, page)
- `block-request.json` — POST /api/contractors/block

## UNCONFIRMED
- Legacy HAR capture: open contractors.jsp, apply filter, next page, block.
- How to verify: DevTools Network → save request/response.
