# Margin payloads

Place here request/response samples for parity:

- `lookups.request.json` / `lookups.response.json` — lookup/dropdown data
- `grid-fetch.request.json` / `grid-fetch.response.json` — main grid request (filters, page, sort) and response (rows, total)
- `export.request.json` / `export.response.json` (if export exists)

Source: capture from legacy screen (network tab) or Agent-Plan output.  
Binary screenshots: do not store in repo (see screenshots/README.md).
